# 2. MCP 프로토콜의 핵심 기능

[← 목차로 돌아가기](./0.목차.md) | [← 이전: 개요](./1.개요.md)

---

MCP는 크게 4가지 핵심 기능을 제공합니다.

| 기능 | 한마디 정리 | 방향 |
|------|-------------|------|
| **Tools** | AI가 실행하는 액션 (생성/수정/삭제) | Client → Server |
| **Resources** | AI가 읽어오는 데이터 (조회) | Client → Server |
| **Prompts** | 재사용 가능한 프롬프트 템플릿 | Client → Server |
| **Sampling** | 서버가 사용자에게 확인 요청 (Human-in-the-loop) | Server → Client |

---

## 2.1 Tools (도구)

**AI가 실행할 수 있는 함수/액션**

### 💡 사용자 → AI → 도구 호출 흐름

```
사용자: "Jira에 '로그인 버그' 이슈 만들어줘"
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI (LLM)                                                       │
│  ① 의도 파악: "이슈를 생성하려는 거구나"                         │
│  ② 도구 탐색: tools/list에서 create_jira_issue 발견             │
│  ③ 파라미터 확인: project 필수인데 없음 → 사용자에게 질문       │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
사용자: "프로젝트는 ABC야"
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI → MCP Server: tools/call                                    │
│  { "name": "create_jira_issue",                                 │
│    "arguments": { "project": "ABC", "summary": "로그인 버그" } } │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server → AI: 결과 반환                                     │
│  { "content": [{ "text": "이슈 ABC-123 생성됨" }] }             │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
AI → 사용자: "ABC-123 이슈를 생성했습니다!"
```

### 🔧 MCP 서버에서 Tool 정의 예시

```typescript
server.registerTool(
  'create_jira_issue',          // 도구 이름
  {
    description: 'Jira 이슈를 생성합니다',  // AI가 도구 선택 시 참고
    inputSchema: {
      project: z.string(),      // 필수 파라미터
      summary: z.string(),
      description: z.string().optional()
    }
  },
  async (args) => {             // 실행 로직
    const issue = await jiraAPI.createIssue(args);
    return { content: [{ type: 'text', text: `이슈 ${issue.key} 생성됨` }] };
  }
);
```

### 🎯 AI가 도구를 선택하는 기준

| 판단 요소 | 역할 | 예시 |
|-----------|------|------|
| `description` | 도구의 용도 설명 → AI가 매칭에 사용 | "Jira 이슈를 생성합니다" |
| `inputSchema` | 필요한 파라미터 정의 | `required: ["project", "summary"]` |
| `name` | 도구 식별자 (보조적) | `create_jira_issue` |

**클라이언트(AI)의 호출 순서:**
1. `tools/list` 호출 → 사용 가능한 도구 목록 조회
2. 사용자 요청 분석 → description 기반으로 적합한 도구 선택
3. `tools/call` 호출 → 도구 실행
4. 결과 수신 → 자연어로 변환하여 사용자에게 응답

---

## 2.2 Resources (리소스)

**AI가 읽을 수 있는 데이터/컨텍스트**

### 💡 사용자 → AI → 리소스 조회 흐름

```
사용자: "ABC 프로젝트 이슈 현황 알려줘"
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI (LLM)                                                       │
│  ① 의도 파악: "이슈 목록을 조회하려는 거구나"                    │
│  ② 리소스 탐색: resources/list에서 jira://project/ABC/issues 발견│
│  ③ 리소스 읽기 결정                                             │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI → MCP Server: resources/read                                │
│  { "uri": "jira://project/ABC/issues" }                         │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server → AI: 데이터 반환                                   │
│  { "contents": [{ "text": "[{\"key\":\"ABC-1\",...}]" }] }      │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
AI → 사용자: "ABC 프로젝트에 3개 이슈가 있습니다.
             - ABC-1: 로그인 버그 (진행중)
             - ABC-2: UI 개선 (완료)
             - ABC-3: 성능 최적화 (대기)"
```

### 🔧 MCP 서버에서 Resource 정의 예시

```typescript
server.registerResource(
  'jira://project/ABC/issues',  // 리소스 URI (고유 식별자)
  {
    name: 'ABC 프로젝트 이슈 목록',
    mimeType: 'application/json'
  },
  async () => {
    const issues = await jiraAPI.getIssues('ABC');
    return { contents: [{ text: JSON.stringify(issues) }] };
  }
);
```

### ⚖️ Tools vs Resources 비교

| 구분 | Tools | Resources |
|------|-------|-----------|
| **목적** | **액션 수행** (생성, 수정, 삭제) | **데이터 읽기** (조회) |
| **예시** | 이슈 생성, 메시지 전송 | 이슈 목록, 파일 내용 |
| **호출** | `tools/call` | `resources/read` |
| **부작용** | 있음 (상태 변경) | 없음 (읽기 전용) |

> 💡 **왜 구분할까?** AI가 "읽기 전용" 작업과 "상태 변경" 작업을 명확히 구분할 수 있어, 실수로 데이터를 변경하는 것을 방지합니다.

### 🔔 Resource Subscribe (리소스 구독)

리소스는 단순 조회(`resources/read`) 외에도, **변경 사항을 실시간으로 알림받는 구독(Subscribe) 기능**을 제공합니다.

```
┌──────────────┐                    ┌──────────────┐
│   Client     │                    │  MCP Server  │
│  (예: AI)    │                    │              │
└──────┬───────┘                    └──────┬───────┘
       │                                   │
       │  1. resources/subscribe           │
       │   (file:///config.json 구독)      │
       │ ─────────────────────────────────>│
       │                                   │
       │  2. 확인 응답                      │
       │ <─────────────────────────────────│
       │                                   │
       │        ... 시간 경과 ...          │
       │                                   │
       │  3. notifications/resources/updated │
       │   (config.json이 변경됨!)          │
       │ <─────────────────────────────────│
       │                                   │
       │  4. resources/read                │
       │   (변경된 내용 읽기)               │
       │ ─────────────────────────────────>│
```

| 시나리오 | 구독 대상 예시 | 효과 |
|---------|--------------|------|
| 설정 파일 모니터링 | `file:///app/config.json` | 설정 변경 시 자동 반영 |
| 로그 실시간 확인 | `logs://app/latest` | 새 로그 발생 시 알림 |
| DB 스키마 변경 감지 | `db://schema/users` | 테이블 구조 변경 시 알림 |

> 💡 **Capability 선언 필요**: 서버가 `initialize` 응답에서 `resources.subscribe: true`를 선언해야 클라이언트가 구독 기능을 사용할 수 있습니다. 이 선언이 없으면 클라이언트는 구독을 지원하지 않는다고 판단하고 시도하지 않습니다.

---

## 2.3 Prompts (프롬프트 템플릿)

**재사용 가능한 프롬프트 패턴**

### 💡 사용자 → AI → 프롬프트 활용 흐름

```
[시나리오: 사용자가 코드 리뷰를 자주 요청함]

사용자: "이 Python 코드 리뷰해줘" + 코드 첨부
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI (LLM)                                                       │
│  ① prompts/list에서 code_review 템플릿 발견                     │
│  ② 템플릿 적용: language="Python", code=사용자 코드              │
│  ③ 표준화된 리뷰 형식으로 응답 생성                              │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI → MCP Server: prompts/get                                   │
│  { "name": "code_review",                                       │
│    "arguments": { "language": "Python", "code": "def foo():..." } }│
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server → AI: 구조화된 프롬프트 반환                         │
│  { "messages": [{ "role": "user",                               │
│      "content": "다음 Python 코드를 리뷰해주세요:\n\ndef foo()..."}] }│
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
AI → 사용자: (일관된 형식의 코드 리뷰 제공)
"📝 코드 리뷰 결과:
 ✅ 장점: 함수명이 명확함
 ⚠️ 개선점: 에러 처리 추가 필요
 💡 제안: 타입 힌트 추가 권장"
```

### 🔧 MCP 서버에서 Prompt 정의 예시

```typescript
server.registerPrompt(
  'code_review',
  {
    name: 'Code Review',
    description: '코드 리뷰 요청용 템플릿',
    arguments: [
      { name: 'language', required: true },
      { name: 'code', required: true }
    ]
  },
  async ({ language, code }) => {
    return {
      messages: [
        { role: 'user', content: { type: 'text', text: `다음 ${language} 코드를 리뷰해주세요:\n\n${code}` } }
      ]
    };
  }
);
```

### 🎯 Prompts를 사용하는 이유

| 장점 | 설명 |
|------|------|
| **일관성** | 동일한 형식으로 반복 작업 수행 (리뷰, 요약, 번역 등) |
| **재사용** | 한 번 정의하면 여러 상황에서 활용 |
| **표준화** | 팀 전체가 동일한 프롬프트 패턴 사용 가능 |

> 💡 **Tools와 차이점**: Tools는 외부 API를 호출해 **액션을 수행**하지만, Prompts는 AI의 **응답 형식을 구조화**하는 역할입니다.

---

## 2.4 Sampling (역방향 요청)

**서버가 클라이언트에게 질문하는 기능** (Human-in-the-loop)

### 🤔 왜 Sampling이 필요한가?

다른 기능들(Tools, Resources, Prompts)은 모두 **클라이언트 → 서버** 방향입니다. 하지만 때로는 **서버가 클라이언트(사용자)에게 확인을 받아야** 하는 경우가 있습니다.

| 상황 | 설명 |
|------|------|
| **위험한 작업 확인** | "정말로 모든 파일을 삭제할까요?" |
| **추가 정보 필요** | "어떤 브랜치에 커밋할까요?" |
| **중간 결과 검토** | "이 내용으로 메일을 보내도 될까요?" |

```
일반적인 흐름:    Client ──요청──> Server ──응답──> Client

Sampling:        Server ──질문──> Client (사용자에게 확인) ──답변──> Server
                 예: "정말로 모든 파일을 삭제할까요?"
```

### 💡 사용자 관점의 Sampling 경험

```
[시나리오: 사용자가 대량 파일 정리 요청]

사용자: "Downloads 폴더에서 30일 지난 파일 다 삭제해줘"
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI (LLM)                                                       │
│  ① 의도 파악: "오래된 파일을 삭제하려는 거구나"                  │
│  ② 도구 선택: delete_old_files 도구 호출                        │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  AI → MCP Server: tools/call                                    │
│  { "name": "delete_old_files",                                  │
│    "arguments": { "path": "~/Downloads", "days": 30 } }         │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server: 삭제 대상 파일 목록 생성                            │
│  → 47개 파일 발견 (총 2.3GB)                                    │
│  → ⚠️ 위험한 작업! 사용자 확인 필요                             │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼ 🔄 역방향 요청 (Sampling)
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server → AI → 사용자: sampling/createMessage               │
│  "다음 47개 파일(총 2.3GB)을 삭제합니다:                        │
│   - report_2024.pdf (15MB)                                      │
│   - photo_backup.zip (1.2GB)                                    │
│   - ... 45개 더                                                 │
│   계속 진행할까요? [예/아니오]"                                  │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
사용자: "예, 진행해줘" (또는 "아니, 취소해")
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  사용자 → AI → MCP Server: 사용자 응답 전달                      │
│  { "response": "예" }                                           │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│  MCP Server: 사용자가 승인했으므로 삭제 실행                     │
│  → 47개 파일 삭제 완료                                          │
└─────────────────────────────────────────────────────────────────┘
        │
        ▼
AI → 사용자: "47개 파일(2.3GB)을 삭제했습니다!"
```

### ⚖️ Sampling vs 다른 기능 비교

| 구분 | 방향 | 주체 | 용도 |
|------|------|------|------|
| **Tools** | Client → Server | AI가 서버에 요청 | 액션 실행 |
| **Resources** | Client → Server | AI가 서버에 요청 | 데이터 조회 |
| **Prompts** | Client → Server | AI가 서버에 요청 | 템플릿 조회 |
| **Sampling** | **Server → Client** | **서버가 사용자에게 요청** | **확인/추가 입력** |

### ⚠️ 보안 주의사항

Sampling은 **서버가 사용자에게 직접 질문**할 수 있는 강력한 기능입니다. 악의적인 서버가 이를 악용할 수 있습니다:

```
[악용 시나리오]
악성 서버: "시스템 보안을 위해 비밀번호를 입력해주세요"
           → 사용자가 속아서 비밀번호 입력
           → 서버가 비밀번호 탈취!
```

**방어 방안:**
- 신뢰할 수 있는 MCP 서버만 Sampling 허용
- 민감 정보 입력 요청 시 경고 표시
- Sampling 요청 내용 로깅 및 모니터링

### 💡 게이트웨이 환경에서의 Sampling 복잡성

MCPHUB 같은 게이트웨이 환경에서 Sampling 구현은 특히 까다롭습니다.

```
┌─────────────────────────────────────────────────────────────────┐
│              Sampling 라우팅 문제                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   [문제 상황]                                                   │
│   Jira MCP 서버가 "이 내용 확인해주세요"라고                     │
│   sampling/createMessage 요청을 보냅니다.                       │
│                                                                 │
│   MCP Server ──질문──> MCPHUB ──???──> 수천 개의 클라이언트 중 누구?│
│                                                                 │
│   Hub는 이 요청을 유발한 클라이언트가 누구인지 알아야             │
│   올바른 사용자에게 확인 요청을 전달할 수 있습니다.              │
│                                                                 │
│   [해결 방안: Stateful Connection Mapping]                      │
│   ───────────────────────────────────────────────────────────   │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │  Session Table (Redis 등)                               │   │
│   │  ┌─────────────┬──────────────┬───────────────────┐     │   │
│   │  │ Request ID  │ Client Conn  │ MCP Server        │     │   │
│   │  ├─────────────┼──────────────┼───────────────────┤     │   │
│   │  │ req-abc123  │ conn-user-A  │ jira-mcp          │     │   │
│   │  │ req-def456  │ conn-user-B  │ slack-mcp         │     │   │
│   │  └─────────────┴──────────────┴───────────────────┘     │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   1. 클라이언트 요청 시 Request ID와 Connection ID 매핑 저장    │
│   2. MCP 서버의 Sampling 요청에 Request ID 포함                 │
│   3. Hub가 Session Table에서 원래 클라이언트를 찾아 전달        │
│                                                                 │
│   ※ 단순 HTTP 로드밸런싱(Round Robin)으로는 불가능!             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

[다음: 통신 →](./3.통신.md)
